// WareMind AI — Dashboard JS (shared utilities placeholder)
// Main logic is inline in each template for modularity.
// Global utilities and shared state can be added here.

// Chart.js global defaults (optional overrides)
if (typeof Chart !== 'undefined') {
    Chart.defaults.font.family = "'Inter', -apple-system, sans-serif";
    Chart.defaults.color = '#94a3b8';
}
